configuration options may cause compile failed.
This is the configuration options by trying:

../configure --enable-shared --enable-pic --prefix=./x264_install --host=i686-w64-mingw32 --disable-cli --disable-asm

make

make install